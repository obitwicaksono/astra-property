import 'package:flutter/material.dart';

class DetailProp extends StatelessWidget {
  const DetailProp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(children: [Text('data'), Text('data'), Text('data')]),
      ),
    );
  }
}
